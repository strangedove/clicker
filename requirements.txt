# Core dependencies required to run clicker
# Note: This file is for reference. The authoritative dependencies are in pyproject.toml.
# Use `uv sync` or `pip install .` to install from pyproject.toml.
accelerate>=1.4.0
datasets>=3.0.0
deepspeed>=0.18.5
huggingface_hub>=0.25.0
numpy>=1.24.0
packaging>=21.0
pandas>=2.0.0
peft>=0.18.1
psutil>=5.9.0
pyarrow>=15.0.0
pyyaml>=6.0
safetensors>=0.4.0
torch>=2.0.0
transformers==4.57.3
wandb>=0.24.1
